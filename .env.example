# Public browser configuration; authentication belongs at the CE gateway.
# Same-origin default. Leaving this empty also falls back to /api.
# The backend and /api routing must be deployed separately.
# Advanced connection settings in the browser can override this value.
VITE_CE_API_BASE_URL=/api
